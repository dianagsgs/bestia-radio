{
  "name": "labestia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next --openssl-legacy-provider start -H 0.0.0.0 -p ${PORT:-8080}",
    "lint": "next lint",
    "export": "npm run build && next export && mv out _static"
  },
  "dependencies": {
    "@google/model-viewer": "^1.8.0",
    "cross-env": "^7.0.3",
    "jquery": "^3.6.0",
    "next": "^11.1.4",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-gtm-module": "^2.0.11",
    "webamp": "^1.5.0"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.0"
  }
}
